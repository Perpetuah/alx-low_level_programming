#include "main.h"
/**
 * _abs - commutes the absolute value of an integer
 * @t: The integer to be commuted
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
